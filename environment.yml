name: sound_simulation
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - h5py=3.14.0
  - matplotlib=3.10.5
  - numpy=2.3.2
  - scipy=1.16.1
  - pytorch=2.8.0
  - tqdm=4.67.1
  - pyvista
  # Note: CuPy installation is highly dependent on your system's CUDA version.
  # The following is an example for CUDA 12.x. Please adjust if necessary.
  # Check https://docs.cupy.dev/en/stable/install.html for more details.
  - cupy-cuda12x
